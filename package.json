{
  "name": "passonce",
  "version": "1.0.1",
  "private": true,
  "description": "Self-hosted zero-knowledge one-time secret sharing (Node, eingebautes SQLite, keine npm-Abhaengigkeiten)",
  "license": "Apache-2.0",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --experimental-sqlite server.js"
  }
}
